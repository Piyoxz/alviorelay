global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "alvio-relay"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["relay:7880"]
        labels:
          service: "alvio-relay"
          cluster: "local"
